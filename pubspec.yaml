name: flutwid_ui
description: A Flutter UI component library.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.0
  flutter: ">=3.0.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  args: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
  test: ^1.24.0
  
executables:
  flutwid_ui: flutwid_ui

flutter:
  uses-material-design: true
